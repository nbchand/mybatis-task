spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
mybatis:
  mapper-locations: classpath*:**/sql/*.xml
